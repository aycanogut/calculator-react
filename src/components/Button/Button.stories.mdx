import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { NumberButton, OperatorButton, EqualButton } from '../Button/index';

<Meta title="Design System|Buttons" component={NumberButton} />

# Buttons

Buttons are used to trigger actions within an application.

## Usage

Buttons are used to trigger internal actions within your web applications.

## Number Buttons

Primary buttons are used as a call to action and indicate the most important action on a page.

<Preview withToolbar>
  <Story name="Number">
    <NumberButton>Hello world</NumberButton>
  </Story>
</Preview>

## Operator Buttons

Primary buttons are used as a call to action and indicate the most important action on a page.

<Story name="Operator">
  <OperatorButton>Hello world</OperatorButton>
</Story>

## Equal Button

Primary buttons are used as a call to action and indicate the most important action on a page.

<Story name="Equal">
  <EqualButton>Hello world</EqualButton>
</Story>
