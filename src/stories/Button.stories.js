import Button from '../components/Button/Button';

export default {
  title: 'Button',
  component: Button
};

export const Red = () => (
  <Button label="press me" backgroundColor="red">
    Hello Button
  </Button>
);
