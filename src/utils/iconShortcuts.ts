import data from '../data.json';

export const historyIcon = {
  src: data.icons.history.src,
  alt: data.icons.history.alt
};

export const backIcon = {
  src: data.icons.back.src,
  alt: data.icons.back.alt
};
